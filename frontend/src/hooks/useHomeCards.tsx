import { useState, useEffect } from "react";

export const useHomeCards = () => {

  const [component, setcomponent] = useState(null);
  const [product_backlog, setproduct_backlog] = useState(null);
  const [SPBI, setSPBI] = useState(null);
  const [RE, setRE] = useState(null);

  return <div>useHomeCards</div>;
};

export default useHomeCards;
